# Culture-specific keyword resources

`AutomatedMetrics` looks for one JSON file per culture in this directory, for
example `chinese.json`. Those research resources are not included in this
prototype release. When a file is absent, dimension-dependent Tier I metrics
are incomplete and the runtime emits a warning.

Do not interpret fallback values as reproduction of the paper's reported
evaluation results.
